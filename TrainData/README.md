# TrainData

`train-inputs.txt` contains 10 short text samples for one-step and small-batch training bring-up.

`whoami-1000.tsv` format:
- one sample per line
- `prompt<TAB>target`
- used by `scripts/Train.WhoAmI.AndExportDat.fsx --train-data ...`
